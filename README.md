# Definir variables para los gastos
Transporte = float(input(“Ingrese los gastos en transporte: “))
Comida = float(input(“Ingrese los gastos en comida: “))
Libros = float(input(“Ingrese los gastos en libros: “))
Alquiler = float(input(“Ingrese los gastos en alquiler: “))
Otros = float(input(“Ingrese otros gastos: “))

# Calcular el total de gastos
Total_gastos = transporte + comida + libros + alquiler + otros

# Imprimir el total de gastos
Print(f”El total de gastos mensuales
